# Normalize line endings across platforms: store text files as LF in the
# repository and check them out as LF everywhere. This silences Git's
# Windows "LF will be replaced by CRLF" warnings and keeps diffs clean.
# There's no build step that depends on line endings.
* text=auto eol=lf

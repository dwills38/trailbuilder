{
  "//": "Type-checks the plain-JS sources via JSDoc. No build step: noEmit is on, nothing is compiled or shipped. Run with `npm run typecheck` (tsc --noEmit).",
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020"],
    "module": "nodenext",
    "moduleResolution": "nodenext",
    "types": ["node", "vitest/globals"],

    "allowJs": true,
    "checkJs": true,
    "noEmit": true,

    "strict": true,
    "useUnknownInCatchVariables": false,

    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  },
  "include": ["src/**/*.js", "test/**/*.js", "validate.js"],
  "//exclude": "Browser glue is not type-checked, same as the index.html inline script: src/account.js uses DOM + cross-file script globals (window.supabase, config vars) that this Node-lib/script-global config can't resolve, and src/vendor/** is a minified third-party bundle. The pure logic (compat/schema/validate) + tests remain fully checked.",
  "exclude": ["node_modules", "src/vendor/**", "src/account.js"]
}
